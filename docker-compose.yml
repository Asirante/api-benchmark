services:
  db:
    image: postgres:15-alpine
    container_name: benchmark_db
    command: postgres -c 'max_connections=5000' -c 'shared_buffers=1GB' -c 'work_mem=32MB' -c 'synchronous_commit=off'
    environment:
      POSTGRES_USER: benchmark_user
      POSTGRES_PASSWORD: benchmark_password
      POSTGRES_DB: olist_db
    ports:
      - "5432:5432"
    volumes:
      - ./init-db:/docker-entrypoint-initdb.d
      - ./data:/data
      - pgdata:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M

  rest-api:
    build: 
      context: .
      dockerfile: Dockerfile.rest
    container_name: benchmark_rest
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=benchmark_user
      - DB_PASS=benchmark_password
      - DB_NAME=olist_db
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M

  graphql-api:
    build: 
      context: .
      dockerfile: Dockerfile.graphql
    container_name: benchmark_graphql
    ports:
      - "8081:8081"
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=benchmark_user
      - DB_PASS=benchmark_password
      - DB_NAME=olist_db
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M

  grpc-api:
    build: 
      context: .
      dockerfile: Dockerfile.grpc
    container_name: benchmark_grpc
    ports:
      - "50051:50051"
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=benchmark_user
      - DB_PASS=benchmark_password
      - DB_NAME=olist_db
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M

  envoy:
    image: envoyproxy/envoy:v1.27.0
    container_name: benchmark_envoy
    volumes:
      - ./envoy.yaml:/etc/envoy/envoy.yaml
    ports:
      - "8082:8082" 
      - "9901:9901"
    depends_on:
      - grpc-api
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1024M

  influxdb:
    image: influxdb:1.8
    container_name: benchmark_influxdb
    ports:
      - "8086:8086"
    environment:
      - INFLUXDB_DB=k6
    volumes:
      - influxdb_data:/var/lib/influxdb

volumes:
  pgdata:
  influxdb_data: