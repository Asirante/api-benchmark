# 가설 판정 요약

판정에 쓴 규칙은 모두 **해당 실험 실행 전에 코드와 `environment.md` 에 고정**했고, 실행 이후 상수를 바꾸지 않았습니다.

| 규칙 | 고정 커밋 | 커밋 시각 | 적용 세션 (시작 시각) |
|---|---|---|---|
| 붕괴·포화 구분, 가설 1 대체 판정(포화 구간 풀 비교) | `2ec4977` | 2026-09-16 11:44 | `de_20260916_1154` (11:54) |
| 가설 2 판정, 처리 한계 판정 | `6ff8298` | 2026-09-17 12:18 | `de_e2_20260917_1243` (12:43), `de_cap_20260918_0345` (09-18 03:45) |
| 쿼리 수 보정 조건의 처리 한계 판정(`H2_CAP_DIFF=0.05`, 범위 비중첩) | `e30c9f8` | 2026-09-18 14:22 | `de_caph2_20260918_1436` (14:36) |

`git diff 6ff8298 -- analysis/` 결과가 비어 있어, 판정에 쓴 임계값이 실행 시점과 동일함을 확인했습니다. 마지막 세션 이후 `analysis/followup_tables.py` 에 가한 변경은 집계 오류 수정(요약에 p50·p95 추가, 범위 중첩 판정 헬퍼 누락)뿐이며 임계값은 그대로입니다.

## 가설 1 — 간헐적 붕괴의 원인은 아키텍처가 아니라 커넥션 풀 크기다: **지지**

기준 조건(풀 500:100, 640 rps, 180초, 아키텍처별 3회)에서 붕괴가 재현되어 세 아키텍처 모두 판정 대상이 되었습니다(REST 1/3, GraphQL 2/3, gRPC 1/3 + 포화 1회). 같은 부하에서 풀만 줄이면 붕괴가 사라집니다. 풀 20:20·50:50·100:100 의 **27회 실행 전부 안정**이었고 목표 640 iter/s 를 100% 달성했으며 p99 는 2.5~8.4 ms, DB CPU 스로틀링은 1.7% 이하, ProcArray 대기는 0 이었습니다. 반면 풀 500:100 의 붕괴 실행은 새 DB 연결 6,995~10,137개, ProcArray 대기 273~400 세션, DB 스로틀링 96~99.7%, p99 892~1,212 ms 를 보였습니다. 800 rps 포화 구간에서도 사전 고정 규칙(작은 풀 최선값이 500:100 대비 처리량 +10% 이상 & 3회 범위 비중첩 & p99 더 낮음)을 세 아키텍처 모두 충족했습니다(REST +41.9%, GraphQL +35.2%, gRPC +46.5%). 연결 반복 생성과 동시 세션 상한을 분리한 500:500 조건은 REST 차이 없음, GraphQL 불확실, gRPC 확인으로 일관되지 않았고, 640 rps 에서 500:500 도 무너졌습니다. 따라서 **주 원인은 동시 세션 상한이 큰 것**이며, 연결 반복 생성은 조건에 따라 더해지는 요인으로 해석합니다.

한계: 붕괴는 확률적으로 나타나므로(기준 조건 9회 중 5회) 붕괴 유무 자체는 이진 판정이 아니라 발생 빈도로 읽어야 합니다. 또한 같은 풀 500:100·480 rps(용량의 약 70%)에서는 9회 모두 안정이어서, 붕괴는 **큰 풀 + 용량에 가까운 부하**가 겹칠 때 나타났습니다. 직전 세션(`exp_20260915_030551`)의 포화 전 붕괴 3건이 모두 호스트 swap 구간에 몰려 있었다는 점(swap 없는 31회 중 0건, swap 발생 5회 중 3건)도 함께 기록합니다.

## 가설 2 — GraphQL 의 열위는 프로토콜이 아니라 TC3 의 쿼리 수 때문이다: **p99 에 대해 지지, 처리 한계에 대해서는 부분 지지**

붕괴 영향을 배제한 풀 50:50 에서 GraphQL 의 반복당 DB 쿼리를 24회에서 19회(REST·gRPC 와 동일)로 맞추자, 사전 고정 규칙(p99 30% 이상 감소 & 3회 범위 비중첩)을 두 부하에서 모두 충족했습니다. 640 rps 에서 p99 4.6 ms[3.9–6.0] → 2.6 ms[2.4–2.6](−45%), 800 rps 에서 79.8 ms[68.1–96.9] → 44.3 ms[42.5–47.5](−45%)입니다. 같은 세션의 REST p99 는 36.6 ms, gRPC 는 79.1 ms 였으므로, 보정 후 GraphQL 은 REST 에 근접하고 gRPC 보다 낮습니다. 경로는 DB 작업량입니다. DB CPU 가 147% → 123%(800 rps)로 줄었고 DB 스로틀링은 두 조건 모두 1% 이하였습니다.

처리량 측면은 두 단계로 판정했습니다. 800 rps 이하에서는 차이가 없습니다. 두 조건 모두 800 iter/s 를 100% 달성했고, 이 구간에서는 세 아키텍처 모두 **API 컨테이너 CPU 가 제한(2코어)에 걸려** 있었습니다(상한 도달 시간 약 95~98%).

처리 한계는 네 조건(REST·gRPC·GraphQL light·GraphQL 기존)을 **같은 세션**에서 풀 50:50 · 800~1100 rps · 각 3회로 재측정해 확정했습니다(세션 `de_caph2_20260918_1436`, 48회, 규칙은 실행 전 커밋 `e30c9f8` 에 고정). 쿼리 수 보정은 처리 한계를 실제로 끌어올립니다. GraphQL 의 최대 달성 처리량이 24쿼리 839 iter/s[822–840] 에서 19쿼리 953 iter/s[924–957] 로 **+13.6%** 올랐고 3회 범위가 겹치지 않습니다. 목표 미달이 시작되는 rate 도 900 → 1000 rps 로 밀렸습니다. 그러나 **보정 후에도 차이가 남습니다.** 같은 세션의 REST 1,034 iter/s[1,024–1,048], gRPC 1,017 iter/s[995–1,046] 에 대해 GraphQL light 는 낮은 쪽 기준(gRPC) 대비 **−6.3%** 이고 범위가 겹치지 않아, 사전 고정 규칙상 **유의하게 낮음**으로 판정됩니다. 따라서 TC3 의 쿼리 수는 GraphQL 처리 한계 열위의 **주된 부분이지만 전부는 아니며**, 남은 약 6% 는 쿼리 수가 아닌 요인(gqlgen 의 응답 구성과 필드 리졸버 실행 비용 등 API 측 CPU 소비)에서 온다고 해석합니다. 네 조건 모두 병목은 API 컨테이너 CPU 였고(상한 도달 시간 93~94%, DB CPU 상한 도달 0%), 1100 rps 에서 네 조건 모두 목표 미달이어서 사전 고정한 중단 규칙에 따라 rate 를 더 올리지 않았습니다.

이로써 이전 판정에서 **보류**로 남겨 두었던 "쿼리 수를 맞추면 처리 한계도 같아지는가" 는 **아니오(차이가 줄지만 남음)** 로 확정되었습니다. 보정 전 측정(세션 `de_cap_20260918_0345`: REST 1,015 / gRPC 983 / GraphQL 821 iter/s)도 방향이 같으며, 세션 간 재현성은 조건별 2~4% 입니다.

한계: 남은 6% 차이의 원인은 이번 설계로 분해하지 못했습니다. light 조건은 TC3 의 쿼리 수만 19회로 맞춘 것이고, 요청 형식(단일 POST, 루트 필드 2개 병렬 실행)과 응답 본문은 여전히 다릅니다. 따라서 "쿼리 수 외의 요인"은 후보로만 제시하며, 프로파일링 없이 gqlgen 의 특정 단계를 지목하지는 않습니다.

주의: 800 rps 의 p99 는 세션 간 변동이 큽니다(GraphQL 기존 조건이 `de_20260916_1154` 에서는 170 ms, `de_e2_20260917_1243` 에서는 79.8 ms). 따라서 가설 2 판정은 같은 세션 안의 비교로만 수행했고, 논문에서도 800 rps p99 의 세션 간 비교는 피해야 합니다.
